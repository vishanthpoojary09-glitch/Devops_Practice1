name: CI/CD Demo
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: echo "npm install softawre"
      - name: Run tests
        run: echo "npm install software  testing"
      - name: Deploy
        run: echo "Deployed successfully!"
